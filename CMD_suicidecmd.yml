"suicide commands":
    type: world
    events:
       on suicide command:
          - announce "<red><player.name> decided to end it all!"
          - playeffect location:<player.location> effect:red_dust qty:500
          - execute as_player "kill"